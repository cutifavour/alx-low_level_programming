#include <studio.h>

/**
 * main - print the alphabet.
 *
 * Return: Always 0(Success)
 */
int main(void)

{ char alp[26] = "abcdefghijklmnopqrstuvwxyz";
	int i;

	for (i = 0; i < 2b; i++)
	{putchar(alp[i]); }
	putchar('\n');
	return (0);
}
